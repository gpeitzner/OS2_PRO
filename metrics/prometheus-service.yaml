apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: prometheus
spec:
  selector:
    app: prometheus-prometheus-prometheus-oper-prometheus-0
  ports:
    - port: 9090
      targetPort: 9090
  type: LoadBalancer